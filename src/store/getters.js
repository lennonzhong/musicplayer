export const singer = (state) => {
    return state.singer;
}

export const singerinfo = (state) => {
    return state.singerinfo;
}

export const singerSongs = (state) => {
    return state.singerSongs;
}

export const playCurrentObj = (state) => {
    return state.playCurrentObj;
}

export const playStatus = (state)=>{
    return state.playstate;
}